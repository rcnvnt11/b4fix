sudo apt update && sudo apt upgrade -y

apt update && apt upgrade -y

curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs
node -v

npm install / npm install axios ethers prompt-sync

node jwt_auto.js


 #UPGRADE NODE.JS DAHULU
apt remove nodejs -y
apt purge nodejs -y
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs
